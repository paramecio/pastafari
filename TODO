# Add codename to server group for future Pastafari CLI (the old Protozoo).

# Add total servers down in servers list.

--------------
Future
--------------

* When add a server, give the election for create a user with ssh privileges and access via ssh key provided.


